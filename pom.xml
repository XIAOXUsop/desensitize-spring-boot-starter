<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.xiaoxu</groupId>
    <artifactId>desensitize-spring-boot-starter</artifactId>
    <version>0.6.1</version>
    <packaging>jar</packaging>

    <name>desensitize-spring-boot-starter</name>
    <description>注解式敏感数据脱敏 Spring Boot Starter（展示态，Jackson 序列化层）</description>
    <url>https://github.com/XIAOXUsop/desensitize-spring-boot-starter</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>XIAOXUsop</id>
            <name>XIAOXUsop</name>
            <url>https://github.com/XIAOXUsop</url>
        </developer>
    </developers>

    <!-- Central 要求 SCM 指向可访问的仓库；tag 与 POM 版本一致才能通过校验 -->
    <scm>
        <connection>scm:git:https://github.com/XIAOXUsop/desensitize-spring-boot-starter.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/XIAOXUsop/desensitize-spring-boot-starter.git</developerConnection>
        <url>https://github.com/XIAOXUsop/desensitize-spring-boot-starter</url>
        <tag>v${project.version}</tag>
    </scm>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-boot.version>3.5.13</spring-boot.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- 测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ApplicationContextRunner：验证自动配置在真实上下文中生效 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ApplicationContextRunner 的 AssertProvider 需要 AssertJ -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
            <!--
              把 LICENSE 打进 jar 的 META-INF。
              Maven 默认不这么做，于是发布的产物里就没有许可证文本——
              一个可自由再分发的 jar 必须自带它的许可条件。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>copy-license-into-jar</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/META-INF</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}</directory>
                                    <includes>
                                        <include>LICENSE</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
          发布到 Maven Central 用的 profile。**默认不参与构建**：
          签名需要 GPG 私钥，源码 / Javadoc jar 也会拖慢日常迭代。

          用法：mvn -Prelease -DskipTests deploy
          凭据来自 settings.xml 里 id=central 的 server（Sonatype 用户令牌）。
        -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <!--
                          刻意不用 3.6+：那些版本要求 Maven ≥ 3.6.3，而本仓库没有
                          Maven Wrapper（ctxpress / amlagent 有），使用者用的是自己装的
                          Maven。3.5.0 在 Maven 3.3.9 与 CI 的现代 Maven 上都能跑。
                        -->
                        <version>3.5.0</version>
                        <configuration>
                            <!-- 注释里有中文与代码片段，doclint 的严格模式会让发布卡在文档格式上 -->
                            <doclint>none</doclint>
                            <quiet>true</quiet>
                            <!-- 显式给源级别：不写的话 javadoc 会退回默认的 8，
                                 于是在 switch 表达式上直接报错 -->
                            <source>21</source>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <!-- 先传到 Portal 停在待发布状态，确认无误再点发布 -->
                            <autoPublish>false</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>